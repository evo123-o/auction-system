# 使用指南 (Usage Guide)

## 启动系统

### 1. 启动后端

确保已配置好数据库，然后运行:

```bash
mvn spring-boot:run
```

后端将在 http://localhost:8080 启动。

### 2. 启动前端

在新的终端窗口中:

```bash
cd frontend
npm run dev
```

前端将在 http://localhost:3000 启动。浏览器将自动打开。

## 主要功能

### 1. 登录系统

访问 http://localhost:3000/login

默认测试账号:
- 用户名: `testuser`
- 密码: `password123`

或管理员账号:
- 用户名: `admin`
- 密码: `password123`

### 2. 浏览拍品

- 主页显示所有拍品列表
- 可以通过标题搜索拍品
- 可以按状态筛选拍品
- 点击拍品卡片查看详情

### 3. 创建拍品

登录后:
1. 点击导航栏的"创建拍品"按钮
2. 填写拍品信息:
   - 标题 (必填)
   - 描述 (必填)
   - 起拍价 (必填)
   - 分类 (可选)
   - 保留价 (可选)
   - 图片 (可选)
   - 开始/结束时间 (可选)
3. 点击"创建拍品"按钮提交

### 4. 查看拍品详情

- 点击任意拍品进入详情页
- 可以看到:
  - 拍品图片
  - 详细描述
  - 当前价格和起拍价
  - 拍品状态
  - 出价历史 (需要登录)
- 如果拍品状态为"ACTIVE"，可以出价

### 5. 出价

在拍品详情页:
1. 确保已登录
2. 在出价输入框输入金额
3. 金额必须高于当前价格
4. 点击"出价"按钮
5. 出价成功后会看到更新的价格

### 6. 管理我的拍品

登录后:
1. 点击导航栏的"我的"按钮
2. 可以看到自己创建的所有拍品
3. 对每个拍品可以:
   - 查看详情
   - 编辑信息
   - 删除拍品

### 7. 编辑拍品

在"我的拍品"页面:
1. 点击要编辑的拍品的"编辑"按钮
2. 修改拍品信息
3. 可以上传新图片
4. 点击"更新拍品"保存更改

### 8. 登出

点击导航栏右上角的"退出"按钮即可登出系统。

## 页面说明

### 主页 (/)
- 展示所有拍品的卡片式列表
- 支持搜索和筛选
- 分页显示

### 登录页 (/login)
- 用户名和密码登录
- 登录成功后跳转到原页面或主页

### 拍品详情 (/items/:id)
- 显示拍品完整信息
- 支持出价功能
- 显示出价历史

### 创建拍品 (/items/create)
- 需要登录
- 表单式界面创建新拍品

### 编辑拍品 (/items/:id/edit)
- 需要登录且为拍品创建者
- 编辑现有拍品信息

### 我的拍品 (/dashboard)
- 需要登录
- 展示当前用户创建的所有拍品
- 提供快速管理操作

## 注意事项

1. **出价规则**: 出价金额必须高于当前价格
2. **权限控制**: 只有创建者可以编辑和删除自己的拍品
3. **图片上传**: 支持常见图片格式 (jpg, png, gif等)
4. **状态说明**:
   - PENDING: 待审核
   - APPROVED: 已批准
   - ACTIVE: 进行中
   - CLOSED: 已结束

## 开发者信息

- 前端框架: Vue 3
- 构建工具: Vite
- 状态管理: Pinia
- 路由: Vue Router 4
- HTTP 客户端: Axios

## 常见问题

### Q: 无法登录？
A: 检查是否已在数据库中创建测试用户，密码是否正确。

### Q: 出价失败？
A: 确保金额高于当前价格，且拍品状态为 ACTIVE。

### Q: 图片无法显示？
A: 检查后端的文件上传配置和路径映射是否正确。

### Q: 跨域错误？
A: 确保后端已正确配置 CORS，允许前端域名访问。
